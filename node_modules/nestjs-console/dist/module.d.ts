import { INestApplicationContext } from '@nestjs/common';
import { ConsoleScanner } from './scanner';
import { ConsoleService } from './service';
export declare class ConsoleModule {
    protected readonly service: ConsoleService;
    protected scanner: ConsoleScanner;
    constructor(service: ConsoleService);
    scan(app: INestApplicationContext, includedModules?: unknown[]): Promise<void>;
}
//# sourceMappingURL=module.d.ts.map